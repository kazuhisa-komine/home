
$(function () {
  if ($(".top").length > 0) {
    if ($('.top').length > 0) {
  var top = $('.top').offset().top;
}
  }

  // 以下は既存のJS処理（仮定） - ここに必要な追加処理を入れる
  // 例：
  // $('.some-element').on('click', function () {
  //   ...
  // });

});
